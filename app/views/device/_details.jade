mixin details(details, type)
    each value, id in details
        if value
            if typeof value === "object"
                div.detail-group
                    span #{id}
                    mixin details(details[id], type+'.'+id)
            else
                mixin detail(id, value, type)

mixin detail(id, value, type)
    div.detail
        span #{type}.#{id}
        | #{value}